<h1>ブログ一覧</h1>
<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.title %></td>
    <td><%= blog.content %></td>
  </tr>
<% end %>
<%= link_to 'ブログ一覧画面に戻る', blogs_path %>
<%= link_to '新しくブログを投稿する', new_blog_path %>
</table>